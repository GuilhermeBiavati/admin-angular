import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'admin-angular',
  webDir: 'dist/admin-angular',
  bundledWebRuntime: false
};

export default config;
